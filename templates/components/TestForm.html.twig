<div {{ attributes }}>
    {{ form_start(form, {
            attr: {
                    'data-action': 'live#action:prevent',
                    'data-live-action-param': 'save'
            }
    }) }}
        {{ form_row(form.field_name) }}

        <button>Save</button>
    {{ form_end(form) }}
</div>